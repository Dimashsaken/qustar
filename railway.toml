[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["node"]

[build.nixpacks.build]
cmds = [
  "npm ci",
  "npx expo install --fix",
  "npm run build:web"
]

[deploy]
startCommand = "npx serve dist --single --listen 3000" 